# Лабораторная работа по теме "Разворачивание приложение с микросервисной архитектурой"

## Дополнительные материалы
- [Синтаксис Markdown](./extra/markdown-and-extensions.md)
- [Основные команды и работа в терминале Linux](./extra/cli-commands.md)
- [Система контроля версий Git](./extra/git.md)
- [SSH](./extra/ssh.md)
- [Docker](./extra/docker.md)
- [Docker Compose](./extra/docker-compose.md)

## Требования к выполнению работы

- Компьютер с операционной системой **Debian** и установленной платформой для контейнеризации приложений **Docker**

## Цель работы

- Научиться разворачивать контейнеризированные приложения с микросервисной архитектурой
- Научиться пользоваться инструментом docker compose
- Проверить на практике возможность использования микросервисной архитектуры приложений

## Подготовка к проведению практической работы 

Поверить возможность запуска контейнеров на хосте командой:
```bash
docker run --rm hello-world
```

## Проведение лабораторной работы

### Ознакомление с разворачиваемым приложением

Приложение состоит из нескольких компонентов.

#### Backend

**Backend** - python приложение, которое выполняет всю логику работы приложения. Взаимодействие пользователей осуществляется через WEB API.Само по себе приложение не хранит данные (Stateless), но для его работы необходимы следующие компоненты:
- Реляционная база данных **PostgreSQL**, которая будет осуществлять хранение данных в табличном виде. (Разрабатывают сторонние разработчики)
- Нереляционная база данных (key/value) **Redis**, которая будет использована в качестве кеширующего сервиса (временного хранения данных для снижения операций чтения из **PostgreSQL**).

Собирается в контесте `./app` с использование [Dockerfile](./app/Dockerfile)

#### Используемые переменные окружения:

- **REDIS_HOST** - адрес кеширующего сервиса (в docker-compose можно указывать имя сервиса)
- **REDIS_PORT** - порт для подключения к кеширующему сервису (по умолчанию **6379**).
- **REDIS_DB** - имя базы данных кеширующего сервиса (по умолчанию **0**).
- **CACHE_TIMEOUT** - время хранения данных в кеширующем сервисе в секундах (по умолчанию **30**).
- **POSTGRES_HOST** - адрес реляционной базы данных PostgreSQL (в docker-compose можно указывать имя сервиса)
- **POSTGRES_DB** - имя базы PostgreSQL (по умолчанию **shop**).
- **POSTGRES_USER** - имя пользователя для подключения к БД PostgreSQL (по умолчанию **postgres**)
- **POSTGRES_PASSWORD** - пароль для подключения к БД PostgreSQL (по умолчанию **password**)

#### Описание API приложения (Backend)

**Базовый URL:** `http://<адрес_сервера>:8000`

##### 1. Получение списка элементов
*   **Метод:** `GET`
*   **Путь:** `/items`
*   **Описание:** Возвращает список всех элементов из базы данных. Результат кешируется в Redis на 30 секунд.
*   **Успешный ответ (200 OK):**
    ```json
    [
      {"id": 1, "name": "Кофе", "instance_id":"3cc5d459f77e","use_cache":"False"},
      {"id": 2, "name": "Чай", "instance_id":"3cc5d459f77e","use_cache":"False"}
    ]
    ```

##### 2. Создание нового элемента
*   **Метод:** `POST`
*   **Путь:** `/items`
*   **Описание:** Добавляет новый элемент в базу данных. Сбрасывает кеш списка.
*   **Запрос (тело):**
    ```json
    {
      "name": "Пирожок"
    }
    ```
*   **Успешный ответ (200 OK):**
    ```json
    {"id": 3, "name": "Пирожок", "instance_id":"3cc5d459f77e","use_cache":"True"}
    ```

##### 3. Удаление элемента
*   **Метод:** `DELETE`
*   **Путь:** `/items/{id}`
*   **Описание:** Удаляет элемент с указанным `id` из базы данных. Сбрасывает кеш списка.
*   **Параметры пути:**
    *   `id` (integer, обязательный) — идентификатор элемента.
*   **Успешный ответ (200 OK):**
    Возвращает данные удаленного объекта.
    ```json
    {"id": 1, "name": "Кофе", "instance_id":"3cc5d459f77e","use_cache":"False"}
    ```

##### 4. Ошибка (элемент не найден)
*   **Код ответа:** `404 Not Found`
*   **Описание:** Возвращается при попытке удалить элемент, которого нет в базе данных.
*   **Тело ответа:**
    ```json
    {"detail": "Item not found"}
    ```

### Frontend

**Frontend** - Web интерфейс написанный на TypeScript, отвечающий за визуализацию данных. Также является **Stateless** приложением. Служит для работы с API Backand через графический интерфейс. Для работы ничего нужно (если Backend недоступен, то не будет данных). При этом без этого компонента будет доступна работа напрямую с API Backend приложения.

Собирается в контесте `./frontend` с использование [Dockerfile](./frontend/Dockerfile)

### PostgreSQL

Реляционная база данных. Используется нашим приложением для хранения данных в табличном виде. Это StateFull (требуется хранение постоянных данных) компонент.

[Ссылка на образ](https://hub.docker.com/_/postgres)

#### Используемые переменные окружения

- **POSTGRES_DB** - имя базы создаваемой БД.
- **POSTGRES_USER** - имя пользователя для подключения к БД
- **POSTGRES_PASSWORD** - пароль для подключения к БД PostgreSQL

### Redis

Key/Value БД, которая в нашем проекте используется для кеширования данных. StateLess приложение. При отсутствии кеша или истечения таймаута берутся данные из БД PostgreSQL и записываются в кеш.

[Ссылка на образ](https://hub.docker.com/_/redis)

### Nginx

Web сервер Nginx используется как проксисервер (точка входа пользователей в приложение) и балансировщик нагрузки (если несколько контейнеров с бекендом). Требует файл конфигурации. Его конфигурация описана в [директории](./nginx/conf.d/). Это единственный сервис, который будет принимать запросы из вне.

## Подготовка docker-compose

Первым шагом выполнения работы будет заполнение **docker-compose** файла.

Так как у нас есть StateFull сервис (PostgreSQL) необходимо создать для него том хранения данных. Опишем том с именем **db_data** [Подсказка](./extra/docker-compose.md#типы-томов)

Также необходимо создать 2 сети с именами **backend-net** и **frontend-net** с типо драйвера **bridge** . Для этого опишем их в docker-compose файле [Подсказка](./extra/docker-compose.md#3-настройки-сетей-networks)

Далее опишем сервисы (контейнеры) которые необходмиы для выполнения приложения:

### Сервис db

Сервису с именем **db** необходимо задать следующую конфигурацию:
- Образ **postgres:15-alpine**. [Подсказка](./extra/docker-compose.md#2-настройки-сервисов-services)
- Переменные окружения: [Подсказка](./extra/docker-compose.md#переменные-окружения)
    * **POSTGRES_USER** - ваша фамилия
    * **POSTGRES_DB** - ваше имя
    * **POSTGRES_PASSWORD** - ваша группа
- Подключить ранее созданный том с именем **db_data** с точкой монтирования в `/var/lib/postgresql/data` [Подсказка](./extra/docker-compose.md#проброс-портов-и-томов)
- Сервис должен быть подключен к ранее созданой сети **backend-net** [Подсказка](./extra/docker-compose.md#сетевые-настройки)

### Сервис redis

Сервису с именем **redis** необходимо задать следующую конфигурацию:
- Образ **redis:7-alpine**. 
- Сервис должен быть подключен к ранее созданой сети **backend-net**

### Сервис app

Сервису с именем **app** необходимо задать следующую конфигурацию:

- Сборка в требуемом контексте. [Подсказка](./extra/docker-compose.md#2-настройки-сервисов-services)
- Переменные окружения:
    * **POSTGRES_USER** - пользователь для подключения к БД (тот же что и указан для сервиса db)
    * **POSTGRES_PASSWORD** - пароль для подключения к БД (тот же что и указан для сервиса db)
    * **POSTGRES_DB** - имя базы данных к БД (тот же что и указан для сервиса db)
    * **POSTGRES_HOST** - узел с БД PostgreSQL к которому будет происходить подключение (в docker-compose может использоваться имя сервиса как имя узла)
    * **REDIS_HOST** - узел с БД Redis к которому будет происходить подключение (в docker-compose может использоваться имя сервиса как имя узла)
- Указать сервисы, которые необходимы для работы текущего сервиса (зависимости, после старта которых производить запуск текущего сервиса), а именно **redis** и **db** [Подсказка](./extra/docker-compose.md#зависимости-и-порядок-запуска)
- Сервис должен быть подключен к ранее созданой сети **backend-net**
- Количество реплик (экземпляров контейнера) 3. [Подсказка](./extra/docker-compose.md#масштабирование-и-перезапуск-для-docker-swarm)

### Сервис frontend

Сервису с именем **frontend** необходимо задать следующую конфигурацию:

- Сборка в требуемом контексте.
- Сервис должен быть подключен к ранее созданой сети **frontend-net**

### Сервис nginx

Сервису с именем **nginx** необходимо задать следующую конфигурацию:

- Образ **nginx:stable-alpine**.
- В контейнер должна быть проброшена директория с конфигурацией `./nginx/conf.d` в `/etc/nginx/conf.d` [Подсказка](./extra/docker-compose.md#проброс-портов-и-томов)
- Должен быть проброшен порт хоста **8080** на порт контейнера **80**. [Подсказка](./extra/docker-compose.md#проброс-портов-и-томов)
- Указать сервисы, которые необходимы для работы текущего сервиса **app** и **frontend**.
- Сервис должен быть подключен к ранее созданым ранее сетям **backend-net** и **frontend-net**.

## Запуск и проверка приложения

Когда сформирован полностью docker-compose файл необходимо выполнить его сборку и запуск командой:
```bash
docker compose up --build -d 
```

В случае, если файл docker-compose сформирован корректно, в [WEB интерфейсе](http://localhost) вы увидете список товаров (будет как минимум 2 товара). Также при повторном посещении страницы (обновлении страницы) **Используется кеш** имеет значение **True**, а **Ответ от инстанса** меняется. Также должно работать удаление и добавление товаров.

В данном случае **Ответ от инстанса** это id контейнера **app** ответ с которого приходит (балансировщик нагрузки постоянно меняет контейнеры, на которые проксируются запросы для распределения нагрузки). Так как запущено 3 экземпляра контейнера **app** id должно быть 3. Все полученные id нужно записать в разделе [Полученные результаты](#полученные-результаты).


**Используется кеш** это метка, данные взяты из кеша или напрямую из БД. При использовании кеша, данные берутся из высокоскоростной БД **Redis**, которая хранит данные в ОЗУ.

Вам нужно посовершать действия (в том числе бездействие) с объектами в Web-интерфейсе и указать, в каких случаях используется кеш, а в каких - нет.

## Отключение компонентов и проверка работы системы

Первым выключим кеширующий сервис командой:
```bash
docker compose stop redis
```

Далее посмотреть, как изменилось поведение приложения. Обратите внимание, что время возможных задержек не отражают на скорость чтения/записи в БД PostgreSQL, а скорее отражает время, которое тратится на повторное подключение и ожидание ответа от Redis. И это время может быть сокращено при оптимизации кода и введения дополнительных проверок и методов. Полученные наблюдения записать в разделе [Полученные результаты](#полученные-результаты).

После проверки включим обратно сервис **Redis** командой:

```bash
docker compose start redis
```

И проверим вернулось ли поведение программы в обычное состояние.

Далее выключим **frontend** командой:
```bash
docker compose stop frontend
```

После выключения компонента **frontend**, в [WEB интерфейсе](http://localhost) будет ошибка, но приложение продолжит работать. Проверить работу приложения можно с помощью терминала обратившись к API напрямую. Получим список товаров с помощью команды:

```bash
curl http://localhost/items
```

Запишем вывод команды в разделе [Полученные результаты](#полученные-результаты).

Далее добавим через API новый объект:

```bash
curl -X POST -H 'Content-Type: application/json' -d '{"name":"Огурчики"}' http://localhost/items
```

Получим список товаров повторно с помощью команды:

```bash
curl http://localhost/items
```

и зафиксируем список товаров после добавление через API в разделе [Полученные результаты]

После проверки включим обратно сервис **frontend** командой:

```bash
docker compose start frontend
```

Проверим работоспособность приложения и наличие добавленного товара **Огурчики** в web-интерфейсе.

Следующим шагом получим список работающих контейнеров командой:

```bash
docker compose ps
```

Далее выключим 2 из 3-ех контейнеров app командой:

```bash
docker stop lab-docker-2-app-3 lab-docker-2-app-2
```

И проверим, что контейнеры действительно выключились:
```bash
docker compose ps
```

Проверим в [WEB интерфейсе](http://localhost) как изменилась работа приложения после отключения 2-ух из 3-ех контейнерова app. Полученные наблюдения запишем в разделе [Полученные результаты](#полученные-результаты).

После завершения работы завершим работу всх контейнеров командой:
```bash
docker compose down -v
```


### Полученные результаты

- ФИО:

- Группа:

- id инстансов:

- Когда используется кеш:

- Когда не используется кеш:

- Поведение приложения при неработающем сервисе **Redis**:

- Список товаро, полученных через API с помощью утилиты CUrl:

- Список товаро, полученных через API после добавления товара с помощью утилиты CUrl:

- Появился ли товар "Огурчики" в web-интерфейсе после включения?
